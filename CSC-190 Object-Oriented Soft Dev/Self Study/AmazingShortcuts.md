*** // =========== SHORTER IF CONDITION (TRUE OR FALSE) =========== \\ ***
String displayName = (filename == null) ? "UNKNOWN" : filename;
System.out.println(RandomNumber == 10 ? "RandomNumber is 10" : "RandomNumber is not 10";

variable = (condition) ? valueIfTrue : valueIfFalse;

NOTES: THIS SHORTER IF CONDITION IS CALLED TERNARIES





// =========== SHORTER IF CONDITION (TRUE OR FALS E) =========== \\
STREAK!!!